# Custom Rules

If the user types exactly "PTG", automatically run `git add .`, `git commit -m "Auto-commit"`, and `git push https://github.com/Naitik-xd/Hackathon3.git main` (or the respective branch) to push all the code to GitHub without needing further clarification. Ensure that the push exclusively targets the Hackathon3 repository and does not touch any other repository.
