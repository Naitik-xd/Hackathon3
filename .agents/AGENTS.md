# Custom Rules

If the user types exactly "PTG", automatically run `git add .`, `git commit -m "Auto-commit"`, and `git push origin main` (or the respective branch) to push all the code to GitHub without needing further clarification.
